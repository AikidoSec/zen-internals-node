name: CI

on: push

jobs:
  prebuild:
    name: Prebuild ${{ matrix.os }} ${{ matrix.arch }} Node ${{ matrix.node }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, ubuntu-24.04-arm, macos-15-intel, macos-latest, windows-latest]
        # Node 17 excluded: V8 callback doesn't work correctly
        node: [16, 18, 19, 20, 21, 22, 23, 24, 25]
        exclude:
          - os: macos-latest
            node: 16
        include:
          - os: ubuntu-latest
            arch: x64
          - os: ubuntu-24.04-arm
            arch: arm64
          - os: macos-15-intel
            arch: x64
          - os: macos-latest
            arch: arm64
          - os: windows-latest
            arch: x64
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - name: Install dependencies
        run: npm install --ignore-scripts

      # Python 3.12+ removed distutils, but older node-gyp versions need it
      - name: Install setuptools for Python
        if: runner.os == 'macOS'
        run: pip install setuptools

      # Node 17.0.0 headers are missing due to a bug, use 17.9.1 instead
      - name: Build prebuild
        run: npx prebuildify --strip -t ${{ matrix.node == 17 && '17.9.1' || format('{0}.0.0', matrix.node) }}

      - name: Rename prebuild with Node version
        run: |
          cd prebuilds/*/
          mv zen-internals-node.node zen-internals-node.node${{ matrix.node }}.node
        shell: bash

      - name: Run tests
        run: npm test

      - name: Upload prebuild
        uses: actions/upload-artifact@v4
        with:
          name: prebuild-${{ matrix.os }}-${{ matrix.arch }}-node${{ matrix.node }}
          path: prebuilds/
          retention-days: 30
