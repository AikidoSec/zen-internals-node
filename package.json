{
  "name": "zen-internals-node",
  "version": "1.0.0",
  "description": "Hook into eval/new Function() to validate code before compilation",
  "main": "index.js",
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
    "build": "npx prebuildify --strip -t $(node -p \"process.version.slice(1)\")",
    "test": "node test.js && node ip-matcher.test.js",
    "benchmark:ip-matcher": "node benchmarks/ip-matcher.js",
    "benchmark:ip-matcher-vs-blocklist": "node benchmarks/ip-matcher-vs-blocklist.js"
  },
  "files": [
    "src/",
    "index.js",
    "binding.gyp",
    "prebuilds/"
  ],
  "keywords": [
    "eval",
    "function",
    "security",
    "codegen",
    "hook",
    "native"
  ],
  "devDependencies": {
    "prebuildify": "^6.0.1"
  }
}
